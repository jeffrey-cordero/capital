{
  "name": "capital-react",
  "version": "1.0.0",
  "description": "Finance Tracker",
  "scripts": {
    "lint": "npm --prefix client run lint && npm --prefix server run lint",
    "build": "npm --prefix client run build && npm --prefix server run build",
    "start": "npm --prefix client run start && npm --prefix server run start",
    "dev": "npm --prefix client run dev && npm --prefix server run dev"
  },
  "keywords": ["Finance", "Tracker", "Stocks", "Management"],
  "author": "Jeffrey Cordero",
  "license": "MIT"
}
